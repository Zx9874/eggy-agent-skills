# 给 Agent 的升级任务书

> 用户把新版仓库链接发给你并说“升级蛋仔装备”时使用。升级不能覆盖用户自己的玩法文档或静默合并冲突。

## 一、准备新版

1. 把新版仓库克隆或下载到新的临时目录，不覆盖旧安装包。
2. 读取新版 `VERSION`（版本号）和 `release-manifest.json`（发布清单）。
3. 确认用户总工作区存在 `.eggy-agent\install-state.json`（旧安装状态）。
4. 运行新版候选包验证；失败立即停止。

## 二、执行升级

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "<新版安装包根>\scripts\update-eggy-agent.ps1" -WorkspaceRoot "<总工作区根>"
```

升级器先比较旧状态记录的每个文件指纹，以及总工作区和全部登记地图的受管规则区块。只要一处被用户修改、缺失或损坏，就在修改任何文件之前整体停止并列出冲突。

技能当前处于停用状态时，升级器会在停用区更新并继续保持停用，不会为了升级偷偷启用。用户需要使用新版技能时，再运行启用脚本并新建会话。

不得手工把新版覆盖到冲突文件上。先让用户决定：保留本地修改、把修改迁到项目规则，或恢复公开版后再升级。

## 三、升级不会动什么

- 玩法需求。
- 开发计划。
- 资产清单。
- 项目说明。
- 开发日志。
- 规则文件受管区块之外的用户内容。
- 与工具包无关的任何地图代码和资产。

新版文档模板会更新到工具包目录供对比，但不会覆盖地图现有文档。

## 四、升级后

核对版本号、十一项技能、全部地图登记、备份路径和恢复命令。用代理打开总工作区并新建会话；已经加载到旧会话里的技能无法在原会话中替换。

需要回退时，执行升级输出中的恢复命令，不要自行删除整个工作区。
