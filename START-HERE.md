# 给 Agent：从这里开始安装

你正在帮助一位不会编程的蛋仔地图作者安装原点版与世界版共用的开发工具包。不要让用户手工复制文件，也不要临场设计另一套安装方法。

## 只向用户确认三个信息

1. 总工作区目录：包含一张或多张地图工程的共同父目录，用户以后必须用代理打开这个目录。
2. 当前地图工程：名称通常以 `LuaSource_` 开头。
3. 当前地图是单人还是多人。

必须使用大白话提问。用户已经给出其中某项时不要重复问。无法确认单人或多人时停止安装，不能猜。

## 固定执行顺序

1. 把本仓库克隆或下载到临时目录，不要直接在用户地图中解压覆盖。
2. 确认当前包根目录存在 `VERSION`（版本号）、`release-manifest.json`（发布清单）和 `scripts/install-eggy-agent.ps1`（安装脚本）。
3. 阅读 [INSTALL-给agent的安装任务书.md](INSTALL-给agent的安装任务书.md)，使用用户回答的三个值运行安装脚本。
4. 不手动改写技能、规则模板或发布清单来绕过失败。
5. 安装成功后核对十一项共享技能、总工作区与当前地图的受管规则区块、安装状态和地图唯一文档。
6. 用大白话汇报安装了什么、版本号、备份位置、是否建立版本基线，以及用户下一步怎么开始使用。
7. 明确告诉用户：以后要打开总工作区，不能只打开某张地图；关闭当前会话并新建会话后，规则和技能才会完整生效。

## 必须停止的情况

- 原点版与世界版标记冲突，或无法确认是哪一种版本。
- 地图不在总工作区内。
- 地图被更上层版本仓库包住，不是一张地图一个仓库。
- 发现旧位置存在同名技能。
- 发布清单或文件指纹不一致。
- 已有安装被用户修改，应改走升级冲突处理。
- 任何操作需要覆盖用户自己的玩法文档或完整规则文件。

不要通过删除文件、吞掉错误或跳过检查让安装显示成功。
