# 蛋仔 Agent 技能包安装入口

你正在帮助用户安装本仓库的蛋仔 Agent（代理）技能。先读本文件和 `skill-catalog.json`，再按当前任务选择档案；不要把所有技能正文一次性读入上下文，也不要临场发明另一套安装器。

## 先判断三件事

1. **宿主**：当前是 Codex（代码代理）、OpenCode（开放代码代理）还是 ZCode（智谱代码代理）。无法可靠识别时询问用户，不要凭目录名称猜。
2. **任务域**：本轮实际要修改的是地图运行代码，还是编辑器插件、命令行、转换器、文档工具或技能源码。按目标文件判断，不按对话中出现“蛋仔”判断。
3. **地图版本**：如果是地图，确认原点版或世界版。两者是并行产品，不互通；版本标记冲突或只能猜测时停止。

默认安装范围是**项目级**。只有用户明确说“安装到全局”时，才使用全局范围；普通安装不能修改用户全局技能目录。

## 选择档案

- 原点版地图：`map-origin`。
- 世界版地图：`map-world`。
- 蛋仔辅助工具：`tooling`。
- 只需要公共极简实现和接口研究：`common`。
- 用户明确需要封面、节点改名、模型操作或文档转换时，再把对应 `*-optional` 档案加入；不要默认安装全部可选技能。

`eggy-ponytail`（极简开发）可以用于地图和辅助工具；这不代表辅助工具可以读取地图规则。接口不明确时，`eggy-api-research`（接口研究）可以按工具模式使用。

仓库的 `docs/official` 保存原点版和世界版公开手册镜像。只读取当前任务命中的页面，不要为了安装技能通读整套手册；镜像是检索资料，不替代当前工程接口查证。

## 地图安装需要的信息

只向用户询问缺少的信息，每次最多问两个短问题：

- 总工作区：多张地图共同所在的父目录。
- 当前地图工程：通常是 `LuaSource_` 开头的目录。
- 单人或多人。

确认地图位于总工作区内后，脚本会读取工程标记和结构识别版本。不要读取或修改官方生成目录来“帮助识别”。

## 执行顺序

1. 将本仓库放入临时目录或直接使用已下载的仓库目录。
2. 运行 `scripts/validate-release.ps1`（发布校验）；失败就报告并停止，不改清单绕过。
3. 地图使用 `scripts/install-eggy-agent.ps1`，传入 `WorkspaceRoot`（总工作区）、`ProjectPath`（地图工程）和 `PlayerMode`（单人或多人）。辅助工具不传地图参数，使用 `Profile tooling`。
4. 默认安装核心技能；只有用户明确需要时才传 `IncludeOptional`（包含可选技能）。
5. 安装器只在地图工程中检查或建立独立 Git（版本记录）基线，不代表玩法已经正确或通过试玩；已有安装状态和文件均未变化时不会重复写入或创建备份。
6. 安装成功后核对安装状态、规则受管区块和地图唯一文档；只有发生写入时才会有本次备份位置。不要覆盖用户已有需求、计划、资产、README 或开发日志。

## 必须停止

- 版本标记冲突或无法确认原点版/世界版。
- 地图在总工作区之外，或被更上层 Git 仓库包住。
- 已有受管技能、规则区块或状态被用户改过，且无法安全区分。
- 发现旧位置同名技能，可能造成代理同时加载两个版本。
- 任何步骤需要覆盖用户自己的文档、完整规则文件或官方生成文件。

## 安装后工作方式

打开总工作区，不要只打开地图子目录。新建会话后，代理先读地图根 `README.md`、唯一开发计划的当前任务和本轮相关技能，再按接口研究技能的证据顺序查当前 API（接口）。没有用户要求不得启动编辑器或试玩。
