# 20260805版本更新日志

* 周期：2026-07-31 至 2026-08-05
* 起始版本：`docs-public-2026-W31` `6cd200f0`
* 目标版本：`docs-public-2026-W32` `9d40b8d4`

## 摘要

* 新增类型：1
* 删除类型：0
* 变更类型：14
* 新增成员：20
* 删除成员：2
* 契约签名变化：2
* 其中向后兼容的签名变化：1
* 需要重点关注的破坏性变化：3

## 新增类型

* `StoryService`（Service）：剧情对话播放服务，负责控制地图中预设剧情对话的播放流程。作者可通过该服务启动、终止或跳过指定玩家的剧情，并监听剧情开始、结束与跳过事件以响应剧情生命周期变化。

## 公开契约变化

* `Task.Delay` 函数参数/返回值变化（破坏性）：旧 `参数：duration: Float, func: function；返回：function` → 新 `参数：duration: Float, func: function；返回：thread`
* `TweenInfo.New` 函数参数/返回值变化（向后兼容）：旧 `参数：duration: Float, easingStyle: Enums.EasingStyle, easingDirection: Enums.EasingDirection, repeatCount: Int, reverses: Bool, delayTime: Float；返回：TweenInfo` → 新 `参数：duration: Float, easingStyle?: Enums.EasingStyle, easingDirection?: Enums.EasingDirection, repeatCount?: Int, reverses?: Bool, delayTime?: Float；返回：TweenInfo`

## 破坏性变化

* `Camps.AssignCamp` 删除函数：`参数：player: Player；返回：Int`；为玩家分配阵营
* `Camps.GetCampSpawnPoint` 删除函数：`参数：player: Player, campId: Int；返回：Any`；获取指定阵营的出生点
* `Task.Delay` 函数参数/返回值变化：旧 `参数：duration: Float, func: function；返回：function` → 新 `参数：duration: Float, func: function；返回：thread`

## 新增能力

* `StoryService.EndStory` 新增函数（新增类型）：`参数：player: Player, storyID: String；返回：无`；停止剧情
* `StoryService.SkipStory` 新增函数（新增类型）：`参数：player: Player, storyID: String；返回：无`；跳过剧情
* `StoryService.StartStory` 新增函数（新增类型）：`参数：player: Player, storyID: String；返回：无`；开始播放剧情
* `StoryService.OnStoryEnd` 新增事件（新增类型）：`参数：player: Player, storyID: String`；剧情结束时触发（仅服务端）
* `StoryService.OnStorySkip` 新增事件（新增类型）：`参数：player: Player, storyID: String`；剧情跳过时触发（仅服务端）
* `StoryService.OnStoryStart` 新增事件（新增类型）：`参数：player: Player, storyID: String`；剧情开始时触发（仅服务端）
* `Game.SetClipboardText` 新增函数：`参数：text: String；返回：无`；复制文本到剪切板
* `RenderUnit.TransparentRenderBias` 新增属性：`类型：Int；可空：False；只读：False；可编辑：True`；半透明渲染偏置
* `ScreenGui.SimulateSwipe` 新增函数：`参数：startPos: Vector2, endPos: Vector2, duration: Float；返回：无`；模拟屏幕滑动
* `ScreenGui.SimulateTap` 新增函数：`参数：position: Vector2；返回：无`；模拟屏幕点击
* `ScreenGui.TakeScreenshot` 新增函数：`参数：无；返回：String`；截取当前屏幕并保存为 PNG 图片
* `SimpleAreaLight.LightPattern` 新增属性：`类型：String；枚举：[{'value': 'official://skin/50606_0', 'case_name': 'Default', 'display_name': '默认'}, {'value': 'official://skin/50606_1', 'case_name': 'Grille', 'display_name': '格栅'}, {'value': 'official://skin/50606_2', 'case_name': 'StainedGlass', 'display_name': '彩绘玻璃'}, {'value': 'official://skin/50606_3', 'case_name': 'Mottled', 'display_name': '斑驳光点'}, {'value': 'official://skin/50606_4', 'case_name': 'WaterRipple', 'display_name': '水纹'}]；可空：False；只读：False；可编辑：True`；照射图案
* `SimpleAreaShadowLight.Transparency` 新增属性：`类型：Float；可空：False；只读：False；可编辑：True`；模型透明度
* `SimpleLineShadowLight.Transparency` 新增属性：`类型：Float；可空：False；只读：False；可编辑：True`；模型透明度
* `SimplePointShadowLight.Transparency` 新增属性：`类型：Float；可空：False；只读：False；可编辑：True`；模型透明度
* `SimpleSpotShadowLight.Transparency` 新增属性：`类型：Float；可空：False；只读：False；可编辑：True`；模型透明度
* `SpawnLocationUnit.RangeBirth` 新增属性：`类型：Bool；可空：False；只读：False；可编辑：True`；是否在范围内出生
* `TextUnit.Position` 新增属性：`类型：Vector3；可空：False；只读：False；可编辑：True`；位置
* `TextUnit.Rotation` 新增属性：`类型：Quaternion；可空：False；只读：False；可编辑：True`；旋转
* `WorldUnit.TransparentRenderBias` 新增属性：`类型：Int；可空：False；只读：False；可编辑：True`；半透明渲染偏置
