# 20260709版本更新日志

## 概览

* 类型新增: 2
* 适用范围变更: 2
* 属性新增: 3
* 事件新增: 4
* 方法新增: 11
* **合计: 22**

## 类型变更

### 类型新增 (2)

* **Enums.HumanMeshColor**[enum] [枚举 Enum]: HumanMeshColor 枚举定义了角色模型上可染色的四个区域标识，用于指定对模型的哪个颜色通道进行修改。通过该枚举可以精确控制角色外观的局部颜色，配合 SetMeshColor 或 SetModelColor 等方法实现换色功能。
* **Enums.TeleportErrcode**[enum] [枚举 Enum]: TeleportErrcode 枚举定义传送初始化失败或传送结果通知中可能出现的错误码，用于区分成功、失败、地图不存在、服务器已满、权限不足、请求过频、超时等状态。

### 适用范围变更 (2)

* **MessageService**[class] [服务 Service]: 客户端和服务端 → 仅服务端
* **TeleportService**[class] [服务 Service]: 客户端和服务端 → 仅服务端

## 属性变更

### 属性新增 (3)

* HumanController.InertiaEnabled: Bool — 是否启用移动惯性。
* Player.Character: EggyUnit — 玩家控制的单位。
* Players.CharacterAutoLoads: Bool — 控制游戏是否在玩家加入时自动加载其角色单位。

## 方法变更

### 方法新增 (11)

* BitMask:GetBit: BitMask:GetBit(bitIndex: Int) -> Bool — 读取指定 bitIndex 位置是否置位。
* CFrame:LookAt: CFrame:LookAt(target: Vector3, up: Vector3) -> CFrame — 构造一个位于 at 点、LookVector 指向 target 点的 CFrame
* EUIListView:ScrollToItem: EUIListView:ScrollToItem(itemIndex: Int, time: Float) — 将列表视图滚动到指定索引的项。
* EUIListView:ScrollToPercent: EUIListView:ScrollToPercent(percent: Float, time: Float, attenuated: Bool) — 将列表视图滚动到指定的百分比位置。
* HumanUnit:AddMeshByAssetId: HumanUnit:AddMeshByAssetId(assetId: String) — 添加人形蒙皮，为人形单位添加额外的网格资源。
* HumanUnit:RemoveMeshByAssetId: HumanUnit:RemoveMeshByAssetId(assetId: String) — 移除人形蒙皮，移除之前通过 AddMeshByAssetId 添加的网格资源。
* HumanUnit:SetMeshColor: HumanUnit:SetMeshColor(assetId: String, colorKey: Enums.HumanMeshColor, color: Color, materialIndex: Int) — 设置人形蒙皮染色，为指定网格资源的指定材质索引设置颜色。
* HumanUnit:SetModelColor: HumanUnit:SetModelColor(colorKey: Enums.HumanMeshColor, color: Color, materialIndex: Int) — 设置人形染色，为人形单位的指定颜色键设置颜色。
* HumanUnit:SetModelTexture: HumanUnit:SetModelTexture(assetId: String, materialIndex: Int) — 设置人形贴图，为人形单位的指定材质索引设置贴图资源。
* Player:GetCustomEventSignal: Player:GetCustomEventSignal(eventName: String) -> Signal — 获取玩家触发的自定义事件信号。
* World:GetCustomEventSignal: World:GetCustomEventSignal(eventName: String) -> Signal — 获取一个全局自定义事件信号，用于跨脚本通信。

## 事件变更

### 事件新增 (4)

* BaseController.OnJump: Signal<fun(unit: — SpaceUnit)> 当角色起跳时触发，全局事件。回调参数：unit 触发的生物
* SoundUnit.Played: Signal<fun()> — 当 SoundUnit 开始播放时触发此事件。
* SoundUnit.Stopped: Signal<fun()> — 当 SoundUnit 停止播放时触发此事件。
* TeleportService.TeleportInitFailed: Signal<fun(player: — Player, teleportResult: Enums.TeleportErrcode, errorMessage: String, mapId: String, teleportOptions: table)> 当传送初始化失败时在服务端触发，携带失败玩家、错误码、错误信息、目标地图 ID 和传送选项。回调参数：player 传送的玩家, teleportResult 传送结果错误码, errorMessage 错误描述, mapId 目标地图编号, teleportOptions 传送选项 (含 ShouldReserveServer/ServerInstanceId 等字段)
