# 20260827版本更新日志

## 概览

* 类型新增: 2
* 属性新增: 9
* 属性描述变更: 6
* 方法新增: 4
* 方法描述变更: 52
* 方法返回值变更: 1
* 方法重载变更: 1
* **合计: 75**

## 类型变更

### 类型新增 (2)

* **CustomAppearanceService**[class] [服务 Service]: CustomAppearanceService 自定义外观运行时管理
* **SubCustomAppearance**[class] [数据 Data]: 子自定义外观数据，自定义外观（CustomAppearance）中一个子模型的外观描述；记录子模型的模型资源、皮肤、染色区域、挂点、相对主模型的位置/旋转/缩放、材质参数、透明度与阴影投射等属性。仅作为 CustomAppearance 的 SubAppearance（子模型）数组元素使用。

## 属性变更

### 属性新增 (9)

* BaseMotorUnit.InitDelayTime: Float — 初始延迟时间
* BaseMotorUnit.IsActive: Bool — 是否启用
* CurveVelMotorUnit.CurveVelType: Int — 运动类型
* EffectUnit.ForceLoop: Bool — 开启后强制特效循环播放；关闭时按特效资源自带的播放方式执行（资源本身是循环的仍会循环）。对 efx 与 sfx 特效均生效。
* EggyUnit.EnableAppearance: Bool — 使用蛋形外观
* EggyUnit.RenderMeshId: String — 模型资源ID
* LinkEffectUnit.EndBindUnit: Unit — 终点绑定单位
* LinkEffectUnit.UseEndBindUnit: Bool — 终点绑定单位
* WeldConstraint.Enabled: Bool — 用户持久意图（默认 true）。false 时行为等同本约束被销毁：所属装配体移除该组件（按剩余约束拆分，最后一个约束时销毁整个装配体）；true 时若自身+所有连接单元都在 World 下则重新接入。真实生效状态以只读 Active 为准。

### 属性描述变更 (6)

* RenderTriggerUnit.CastShadow: 描述 启用阴影投射 → 控制模型是否向场景投射阴影
* RenderTriggerUnit.LocalTransparencyModifier: 描述 本地透明度乘数 → 本地客户端的透明度乘数，用于第一人称遮挡等本地渲染效果，不同步到其他客户端
* RenderTriggerUnit.ModelAlpha: 描述 模型透明度 → 控制模型的整体透明度，0 为完全透明，1 为完全不透明
* RenderTriggerUnit.ModelBindParent: 描述 启用后，当父节点为 WorldUnit、RenderUnit、TriggerUnit、PhysicsUnit 等支持父子带动的场景单位时，当前单位将整体跟随父节点运动，保持与父节点的相对位置和旋转。运行时可通过脚本设置 .ModelBindParent = true/false 动态切换该行为。 → 启用后，当父节点为 WorldUnit、RenderUnit、TriggerUnit、PhysicsUnit 等支持父子带动的场景单位时，当前模型渲染表现将跟随父节点的变换自动同步。运行时可通过脚本设置 .ModelBindParent = true/false 动态切换该行为。
* RenderTriggerUnit.ModelVisible: 描述 模型可见性 → 控制模型是否可见，隐藏后仍参与物理碰撞
* RenderTriggerUnit.RenderMeshId: 描述 模型预设ID → 模型的资源路径，用于指定渲染使用的网格资源

## 方法变更

### 方法新增 (4)

* HumanUnit:SetMeshTexture: HumanUnit:SetMeshTexture(meshAssetId: String, textureAssetId: String, materialIndex: Int) — 为人形指定蒙皮的材质索引设置贴图，需先通过AddMeshByAssetId添加该蒙皮
* MaterialParam.New: MaterialParam.New(initData: Table) -> MaterialParam — 创建一个材质参数（MaterialParam）；传入属性表时按属性名初始化各材质属性（与 CreateData('MaterialParam', {...}) 一致），省略时使用默认数据创建。 **适用范围**: 客户端和服务端
* OverlapParams.New: OverlapParams.New() -> OverlapParams — 创建 OverlapParams **适用范围**: 客户端和服务端
* RaycastParams.New: RaycastParams.New() -> RaycastParams — 创建 RaycastParams **适用范围**: 客户端和服务端

### 描述变更 (52)

* Axes.New: 描述: 可选名称：X | Y | Z → 可选名称：X | Y | Z **适用范围**: 客户端和服务端
* BoundingBox.New: 描述: 创建轴对齐包围盒 → 创建轴对齐包围盒 **适用范围**: 客户端和服务端
* Color.Intrp: 描述: 线性插值两个颜色 → 线性插值两个颜色 **适用范围**: 客户端和服务端
* Color.Max: 描述: 逐分量取最大值 → 逐分量取最大值 **适用范围**: 客户端和服务端
* Color.Min: 描述: 逐分量取最小值 → 逐分量取最小值 **适用范围**: 客户端和服务端
* Color.New: 描述: 创建一个新的 Color → 创建一个新的 Color **适用范围**: 客户端和服务端
* Color.Step: 描述: 逐分量阶跃比较 → 逐分量阶跃比较 **适用范围**: 客户端和服务端
* Color3.FromHSV: 描述: 从 HSV 创建 → 从 HSV 创建 **适用范围**: 客户端和服务端
* Color3.FromHex: 描述: 从 16 进制颜色串创建（接受 #RRGGBB / RRGGBB / RGB 三种形式） → 从 16 进制颜色串创建（接受 #RRGGBB / RRGGBB / RGB 三种形式） **适用范围**: 客户端和服务端
* Color3.FromRGB: 描述: 从 0-255 整数创建 → 从 0-255 整数创建 **适用范围**: 客户端和服务端
* Color3.New: 描述: 创建 Color3 → 创建 Color3 **适用范围**: 客户端和服务端
* ColorSequence.New: 描述: 创建颜色序列 → 创建颜色序列 **适用范围**: 客户端和服务端
* ColorSequenceKeypoint.New: 描述: 创建关键帧 → 创建关键帧 **适用范围**: 客户端和服务端
* Faces.New: 描述: 可选名称：Right | Top | Back | Left | Bottom | Front → 可选名称：Right | Top | Back | Left | Bottom | Front **适用范围**: 客户端和服务端
* Matrix.FromEulerAngles: 描述: 从欧拉角创建旋转矩阵（ZXY 旋转顺序） → 从欧拉角创建旋转矩阵（ZXY 旋转顺序） **适用范围**: 客户端和服务端
* Matrix.Identity: 描述: 返回单位矩阵（无平移、无旋转、缩放为1） → 返回单位矩阵（无平移、无旋转、缩放为1） **适用范围**: 客户端和服务端
* Matrix.New: 描述: 由 TRS 构造 4x4 矩阵 → 由 TRS 构造 4x4 矩阵 **适用范围**: 客户端和服务端
* Matrix.Zero: 描述: 返回零矩阵（无平移、无旋转、缩放为0） → 返回零矩阵（无平移、无旋转、缩放为0） **适用范围**: 客户端和服务端
* Matrix3x3.ComputeSkewSymmetricMatrixForCrossProduct: 描述: 为叉积构造反对称矩阵 → 为叉积构造反对称矩阵 **适用范围**: 客户端和服务端
* Matrix3x3.Identity: 描述: 返回单位矩阵 → 返回单位矩阵 **适用范围**: 客户端和服务端
* Matrix3x3.New: 描述: 创建 3x3 矩阵（9 个参数，行主序） → 创建 3x3 矩阵（9 个参数，行主序） **适用范围**: 客户端和服务端
* Matrix3x3.Zero: 描述: 返回零矩阵 → 返回零矩阵 **适用范围**: 客户端和服务端
* NumberRange.New: 描述: 创建 NumberRange → 创建 NumberRange **适用范围**: 客户端和服务端
* NumberSequence.New: 描述: 创建数值序列 → 创建数值序列 **适用范围**: 客户端和服务端
* NumberSequenceKeypoint.New: 描述: 创建关键帧 → 创建关键帧 **适用范围**: 客户端和服务端
* Quaternion.FromAxisAngle: 描述: 从轴角创建四元数 → 从轴角创建四元数 **适用范围**: 客户端和服务端
* Quaternion.FromEulerAngles: 描述: 从欧拉角创建四元数（ZXY 旋转顺序） → 从欧拉角创建四元数（ZXY 旋转顺序） **适用范围**: 客户端和服务端
* Quaternion.Identity: 描述: 返回单位四元数 (0, 0, 0, 1) → 返回单位四元数 (0, 0, 0, 1) **适用范围**: 客户端和服务端
* Quaternion.New: 描述: 创建一个新的四元数 → 创建一个新的四元数 **适用范围**: 客户端和服务端
* Quaternion.Nlerp: 描述: 归一化线性插值 → 归一化线性插值 **适用范围**: 客户端和服务端
* Quaternion.Slerp: 描述: 球面线性插值 → 球面线性插值 **适用范围**: 客户端和服务端
* Ray.New: 描述: 创建射线 → 创建射线 **适用范围**: 客户端和服务端
* Rect.New: 描述: 创建矩形 → 创建矩形 **适用范围**: 客户端和服务端
* Region3.New: 描述: 创建 Region3 → 创建 Region3 **适用范围**: 客户端和服务端
* Region3int16.New: 描述: 创建 Region3int16 → 创建 Region3int16 **适用范围**: 客户端和服务端
* Transform.FromAxisAngle: 描述: 从轴角创建 Transform → 从轴角创建 Transform **适用范围**: 客户端和服务端
* Transform.FromEulerAngles: 描述: 从欧拉角创建纯旋转 Transform（ZXY 旋转顺序） → 从欧拉角创建纯旋转 Transform（ZXY 旋转顺序） **适用范围**: 客户端和服务端
* Transform.FromMatrix: 描述: 从 4x4 矩阵创建 Transform → 从 4x4 矩阵创建 Transform **适用范围**: 客户端和服务端
* Transform.Identity: 描述: 返回单位 Transform → 返回单位 Transform **适用范围**: 客户端和服务端
* Transform.InterpolateTransforms: 描述: 插值两个 Transform → 插值两个 Transform **适用范围**: 客户端和服务端
* Transform.LookAt: 描述: 构造朝向目标点的 Transform → 构造朝向目标点的 Transform **适用范围**: 客户端和服务端
* Transform.New: 描述: 创建一个 Transform → 创建一个 Transform **适用范围**: 客户端和服务端
* TweenInfo.New: 描述: 创建 TweenInfo 对象 → 创建 TweenInfo 对象 **适用范围**: 客户端和服务端
* UDim.New: 描述: 创建 UDim → 创建 UDim **适用范围**: 客户端和服务端
* UDim2.FromOffset: 描述: 从 Offset 创建 → 从 Offset 创建 **适用范围**: 客户端和服务端
* UDim2.FromScale: 描述: 从 Scale 创建 → 从 Scale 创建 **适用范围**: 客户端和服务端
* UDim2.New: 描述: 创建 UDim2 → 创建 UDim2 **适用范围**: 客户端和服务端
* Vector2.New: 描述: 创建一个新的 Vector2 → 创建一个新的 Vector2 **适用范围**: 客户端和服务端
* Vector2int16.New: 描述: 创建 Vector2int16 → 创建 Vector2int16 **适用范围**: 客户端和服务端
* Vector3.New: 描述: 创建一个新的 Vector3 → 创建一个新的 Vector3 **适用范围**: 客户端和服务端
* Vector3int16.New: 描述: 创建 Vector3int16 → 创建 Vector3int16 **适用范围**: 客户端和服务端
* Vector4.New: 描述: 创建一个新的 Vector4 → 创建一个新的 Vector4 **适用范围**: 客户端和服务端

### 返回值变更 (1)

* CustomAppearanceService:CreateCustomAppearance: CustomAppearanceService:CreateCustomAppearance(data: CustomAppearance) -> CustomAppearance → CustomAppearanceService:CreateCustomAppearance(data: CustomAppearance) -> String

### 重载变更 (1)

* Game:GetService: 重载变更
