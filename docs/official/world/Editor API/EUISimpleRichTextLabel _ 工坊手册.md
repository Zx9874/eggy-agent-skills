# EUISimpleRichTextLabel

UI简易富文本。需先通过 PrefabService:CreatePrefab("EUISimpleRichTextLabel") 创建预设，再通过 World:CreateAsset(预设ID) 创建单位，字段属性可直接读写。

## 概览

> 继承自 `Unit`；需先通过 `PrefabService:CreatePrefab("EUISimpleRichTextLabel")` 创建预设，再通过 `World:CreateAsset(预设ID)` 创建单位
